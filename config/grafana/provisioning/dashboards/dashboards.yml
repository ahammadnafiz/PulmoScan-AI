apiVersion: 1

# Load every dashboard JSON mounted under /etc/grafana/dashboards.
providers:
  - name: PulmoScan
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/dashboards
