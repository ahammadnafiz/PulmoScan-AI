# Base URL of the PulmoScan API. Defaults to http://localhost:8000 if unset.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Only needed if the API was started with API_KEY set (sent as X-API-Key).
# NEXT_PUBLIC_API_KEY=
